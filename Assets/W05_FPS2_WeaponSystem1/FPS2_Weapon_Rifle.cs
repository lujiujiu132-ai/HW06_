using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Rifle Class (Child Class) - Inherits all features from Base Class
/// 步枪类 (子类) - 继承父类的所有功能
/// </summary>
public class FPS2_Weapon_Rifle : FPS2_WeaponBase
{
    // [EN] This class is empty, but it has all the powers of FPS2_WeaponBase.
    // [CN] 虽然这个类是空的，但它拥有 FPS2_WeaponBase 的所有功能。
    
    // 1. It can Shoot() / 它可以执行 Shoot()
    // 2. It has damage, fireRate, etc. / 它拥有伤害(damage)、射速(fireRate)等属性
    // 3. It uses the 'Full-Auto' Update logic from the parent.
    //    它直接沿用父类的“全自动” Update 逻辑。
}